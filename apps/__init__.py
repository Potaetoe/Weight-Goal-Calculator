"""Front-ends. Each subdirectory is one way of using core/calc_core.py.

`desktop/` is the tkinter app. `web/` is the browser version and is not a
Python package - it is plain static files, published exactly as they sit.
"""

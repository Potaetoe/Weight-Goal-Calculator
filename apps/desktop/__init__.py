"""The tkinter desktop front-end.

Run it from the repo root with `python -m apps.desktop.weight_calculator`,
or double-click `Run Weight Calculator.bat`. It needs the repo root on
sys.path because it imports core.calc_core.
"""
